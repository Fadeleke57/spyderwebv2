# Estimated sizes for different content types
FILE_SIZE_ESTIMATES = {
    "pdf_page": 100 * 1024,  # 100KB per PDF page
    "text_char": 2,          # 2 bytes per character for text content
    "vector": 1024,          # 1KB per vector
}